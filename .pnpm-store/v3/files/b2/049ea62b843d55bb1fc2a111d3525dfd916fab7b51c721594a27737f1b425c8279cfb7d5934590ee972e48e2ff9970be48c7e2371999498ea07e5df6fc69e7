"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StringType = void 0;
const validator_1 = require("validator");
const abstract_type_1 = require("../abstract-type");
var isEmptyValidator = validator_1.default.isEmpty;
var isLengthValidator = validator_1.default.isLength;
class StringType extends abstract_type_1.AbstractType {
    get isEmpty() {
        if (this.isNull) {
            return true;
        }
        return isEmptyValidator(this.value);
    }
    get length() {
        if (this.isNull) {
            return 0;
        }
        return this.value.length;
    }
    isLength(param) {
        if (typeof param.exactly === 'number') {
            param.min = param.exactly;
            param.max = param.exactly;
        }
        return isLengthValidator(this.toString, param);
    }
    get toString() {
        if (this.isNull) {
            return '';
        }
        return this.value;
    }
    filter(value) {
        if (value === null) {
            return null;
        }
        if (typeof value === 'boolean') {
            return value ? 'true' : 'false';
        }
        return `${value}`;
    }
}
exports.StringType = StringType;
//# sourceMappingURL=string-type.js.map